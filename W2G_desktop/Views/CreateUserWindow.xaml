<Window x:Class="W2G_desktop.Views.CreateUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Créer un utilisateur" Height="300" Width="400">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Email -->
            <RowDefinition Height="Auto"/>
            <!-- Username -->
            <RowDefinition Height="Auto"/>
            <!-- Password -->
            <RowDefinition Height="Auto"/>
            <!-- Role -->
            <RowDefinition Height="Auto"/>
            <!-- Error message -->
            <RowDefinition Height="Auto"/>
            <!-- Button -->
        </Grid.RowDefinitions>

        <TextBlock Text="Email:" Grid.Row="0" Margin="0,5"/>
        <TextBox x:Name="EmailTextBox" Grid.Row="0" Margin="80,5,0,5" Width="250"/>

        <TextBlock Text="Nom d'utilisateur:" Grid.Row="1" Margin="0,5"/>
        <TextBox x:Name="UsernameTextBox" Grid.Row="1" Margin="120,5,0,5" Width="210"/>

        <TextBlock Text="Mot de passe:" Grid.Row="2" Margin="0,5"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="2" Margin="100,5,0,5" Width="230"/>

        <TextBlock Text="Rôle:" Grid.Row="3" Margin="0,5"/>
        <ComboBox x:Name="RoleComboBox" Grid.Row="3" Margin="60,5,0,5" Width="270">
            <ComboBoxItem Content="ROLE_ADMIN"/>
            <ComboBoxItem Content="ROLE_TECHNICIAN"/>
            <ComboBoxItem Content="ROLE_ACCOUNTANT"/>
        </ComboBox>

        <TextBlock x:Name="ErrorTextBlock" Grid.Row="4" Foreground="Red" Margin="0,5"/>

        <Button Content="Créer"
        Grid.Row="5"
        Width="100"
        Height="30"
        Margin="0,10,0,0"
        HorizontalAlignment="Left"
        Click="CreateButton_Click"/>
    </Grid>
</Window>