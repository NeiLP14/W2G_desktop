<Page x:Class="W2G_desktop.Pages.CreateBayPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Créer une baie">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Taille:" Grid.Row="0" Margin="0,5"/>
        <TextBox x:Name="SizeTextBox" Grid.Row="0" Margin="80,5,0,5" Width="100"/>

        <TextBlock x:Name="ErrorTextBlock" Grid.Row="1" Foreground="Red" Margin="0,5"/>

        <Button Content="Créer"
                Grid.Row="2"
                Width="100"
                Height="30"
                Margin="0,10,0,0"
                HorizontalAlignment="Left"
                Click="CreateButton_Click"/>
    </Grid>
</Page>