<Page x:Class="W2G_desktop.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Connexion">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Adresse mail" Grid.Row="0"/>
        <TextBox x:Name="EmailTextBox" Grid.Row="0" Margin="0,20,0,10"/>

        <TextBlock Text="Mot de passe" Grid.Row="1"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Margin="0,20,0,10"/>

        <Button Content="Se connecter" Grid.Row="2" Click="LoginButton_Click" Width="120" Height="30"/>
        <TextBlock x:Name="ErrorTextBlock" Grid.Row="3" Foreground="Red" Margin="0,10,0,0"/>
    </Grid>
</Page>