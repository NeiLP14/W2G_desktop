<Page x:Class="W2G_desktop.Pages.EditUserPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Modifier un utilisateur">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Email :" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="EmailTextBox" Grid.Row="0" Grid.Column="1" Width="250"/>

        <TextBlock Text="Nom d'utilisateur :" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="UsernameTextBox" Grid.Row="1" Grid.Column="1" Width="250"/>

        <TextBlock Text="Rôle :" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox x:Name="RoleComboBox" Grid.Row="2" Grid.Column="1" Width="250">
            <ComboBoxItem Content="ROLE_ADMIN"/>
            <ComboBoxItem Content="ROLE_TECHNICIAN"/>
            <ComboBoxItem Content="ROLE_ACCOUNTANT"/>
        </ComboBox>

        <TextBlock x:Name="ErrorTextBlock" Grid.Row="3" Grid.ColumnSpan="2" Foreground="Red" Margin="0,10,0,0"/>

        <StackPanel Orientation="Horizontal" Grid.Row="4" Grid.Column="1" Margin="0,15,0,0">
            <Button Content="Sauvegarder" Width="120" Click="SaveButton_Click" Margin="0,0,10,0"/>
            <Button Content="Annuler" Width="120" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Page>