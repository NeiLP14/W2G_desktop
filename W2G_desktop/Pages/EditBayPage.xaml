<Page x:Class="W2G_desktop.Pages.EditBayPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Modifier Baie">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Label:"/>
            <TextBox x:Name="LabelTextBox" Width="200"/>

            <TextBlock Text="Taille:" Margin="0,10,0,0"/>
            <TextBox x:Name="SizeTextBox" Width="200"/>
        </StackPanel>

        <!-- StackPanel horizontal pour les boutons -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,10,0,0">
            <Button Content="Sauvegarder" Width="120" Click="SaveButton_Click" Margin="0,0,10,0"/>
            <Button Content="Annuler" Width="120" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Page>